import styled from 'styled-components';

export const Container = styled.div`
	position: relative;
`;

export const Field = styled.div`
	display: flex;
	align-items: center;
	cursor: pointer;

	span:first-child {
		font-weight: 700;
		margin-right: 5px;
	}
`;
